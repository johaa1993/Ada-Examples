library project GLFW is

   for Library_Name use "glfw3.ada";
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";

   package Linker is
      for Linker_Options use ("-lglfw3", "-lgdi32", "-lopengl32");
   end Linker;

end GLFW;

